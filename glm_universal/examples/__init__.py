"""Runnable demonstrations of the package.

These are scripts first and modules second: each one has a ``main`` and runs
standalone under ``PYTHONPATH=. python3 glm_universal/examples/<name>.py``.
They are importable so that the test suite can exercise them rather than let
them rot.
"""
