# `RequestProject/GLM/` — machine-checked companion to the GLM package

**97 Lean files**, all building against Mathlib at `v4.28.0` — the
toolchain already pinned by `glm_lean/lean-toolchain` — and all free of `sorry`.
Every theorem below has been checked with `#print axioms`; none depends on
anything beyond `propext`, `Classical.choice` and `Quot.sound`, except the
finite exhaustive checks in `Golay/Sextet.lean` and in the five `Cube/` files
(and the results downstream of them), which are discharged by `native_decide`
and so additionally use `Lean.ofReduceBool` and `Lean.trustCompiler`.

These files are *additive*. They live in a new `RequestProject/GLM/`
directory alongside the existing `RequestProject/GLM.lean`, `GLM2.lean` and
`GLM3.lean`, which they neither import nor modify. The existing lakefile
glob `RequestProject.+` already picks them up, so nothing needs configuring:

```bash
cd glm_lean
lake build
```

## What is proved, and why it is here

| file | subject | why the package cares |
|---|---|---|
| `Constants.lean` | `Y = 1/(π + 2/π)`, `Q = Y + 1/8`, `B = 10`; `TAX(v) = HW(v)·Y + ‖v‖²/8`; `NRCI(v) = B/(B + TAX(v))` | `nrci_eq_one_iff`: NRCI is in `(0, 1]` and equals `1` **exactly** at the zero carrier — perfect coherence is exactly the vacuum. Each coherence regime is shown to be equivalently a band of TAX. |
| `TaxConservation.lean` | `TAX(a ⊕ b) = TAX(a) + TAX(b) − 2·TAX(a ∧ b)` | Holds *exactly* for binary carriers, and `tax_conservation_fails_at_integer_layer` shows it fails one layer up. The first concrete instance of a law with a boundary. |
| `GolayBoundary.lean` | nearest-codeword reading in a minimum-distance-8 code | `snap_boundary_at_three`: unique for error weight ≤ 3, and at weight 4 there are patterns equidistant from two codewords. This is the boundary `snap_to_codeword` runs into, and the one the runtime reports as `ambiguous`. |
| `Permutation.lean` | coordinate permutations | A permutation is an isometry, so nearest-codeword decoding commutes with it. This is why a legacy→canonical *permutation* may be wrapped around a decoder while a general linear isomorphism may not. |
| `Endianness.lean` | bit order | `bitsMSB_eq_bitReverse_bitsLSB`: the MSB-first and LSB-first readings of a stored integer differ exactly by `Fin.revPerm`. Hence `endianness_is_a_frame_choice`: fixing the frame recovers the code without altering a stored bit, and no decoder guarantee is lost. This is the audit finding behind the literal data migration. |
| `Facets.lean` | the six facets | The facet projections are linear, idempotent, mutually orthogonal and complete, so `pythagoras` splits squared distance exactly across facets. This is what makes the runtime's facet attribution exact rather than heuristic. |
| `Sakuma.lean` | the 2A Sakuma product | `sakuma_not_associative`: the two bracketings of a pairwise-2A triple are `−3/32·e₂` and `−3/32·e₀`. The XOR label shortcut it replaced *is* associative, so the replacement was not cosmetic. |
| `Layers.lean` | the general theory | A layer is a `perceive` map. Refinement, visibility, boundary and congruence are derived from it. `boundary_nonempty_iff_new_visible`: information lost at a boundary is *precisely* new expressive power. `descends_iff_congruent` is the exact content of the `can_multiply` flag. |
| `Stack.lean` | the concrete GLM stack | substrate → integer → rational, with worked escalations and a measured loss count on a four-carrier region. |
| `Cumulative.lean` | how a stack is *made* a refinement chain | `cumulative L M` is the layer that keeps `L`'s reading and adds `M`'s. It refines both and is the coarsest layer that does (`refines_cumulative_iff`), what it gains is exactly what the new reading sees (`boundary_cumulative_left`), and a tower built this way is a refinement chain by construction (`cumulativeTower_refines_of_le`). The concrete model is the GLM's own case: `si7Model_not_refines_substrateModel` proves the hole the exponents-only reading has, and `integerModel_refines_substrateModel` that the cumulative integer layer — the one the package now ships — does not. |
| `LayerChain.lean` | the five shipped layers, on the real carrier type | `Cumulative.lean` in miniature made full size: `glmSubstrateLayer`, `glmIntegerLayer`, `glmRationalLayer`, `glmGriessLayer` and `glmUniversalLayer` over `Carrier24 = Fin 24 → ℚ`, each one's `perceive` being exactly the data its Python `measure` compares. `glmSi7Layer_not_refines_glmSubstrateLayer` is the audit's defect on the audit's own pair — the vacuum against a unit on coordinate 10 — and `glmIntegerLayer_separates_unitOutside` is the repair on the same pair. `glmChain_refines_of_le` is `refinement_chain_intact = True` as a theorem, and `glmChain_visible_mono` is what it buys: nothing the substrate can state is lost anywhere up the stack. `boundary_glmIntegerLayer_glmSubstrateLayer_nonempty` shows the repair did not flatten the step. |
| `Escalation.lean` | the same chain, at register scale | What `reasoning/escalation.py` measures on 1,040 carriers, stated so that it holds however the registers grow. A register is a `Naming` from entries to carriers; `entryResolution` counts the entries a layer separates. `entryResolution_le_distinct` is the **resolution ceiling** — no layer resolves more entries than there are distinct carriers, so the 283 entries that share a carrier are beyond every layer by construction, not by a defect. `entryResolution_mono` is the order of the stack: a finer layer never resolves fewer, so the measured 415 → 544 → 757 cannot invert. `entryResolution_eq_of_lossless` says a lossless layer reaches the ceiling exactly, and `glmRationalLayer_congruentOn` with its griess and universal companions is addition descending wherever the view is the carrier. `substrate_addition_not_congruent` and `glmIntegerLayer_addition_not_congruent` are why it does not descend below: a half unit and the vacuum agree under a reading that floors first, and their doubles do not. |
| `NameCoordinate.lean` | lifting the ceiling `Escalation.lean` proves | The 283 entries no layer can separate are a *coordinate* problem, and this file is the theory of the coordinate that fixes it. `namedLayer` reads an entry as the pair of its carrier and a code computed from the entry; `namedLayer_refines_entryLayer` is the widening — the code can split a class and can never merge two — and `entryResolution_le_namedResolution` is the ceiling therefore only rising. `namedResolution_of_injective` is the headline and the reason the headline is cheap: an injective code resolves *every* entry from *any* layer, the 24-bit substrate included, so the measured 415 → 1,040 is forced rather than discovered. The content is in the two bounds below it. `namedResolution_le_mul` says a code taking at most `m` values multiplies a layer's resolution by at most `m`, and `card_le_of_codeInjOn` is its sharp form inside one collision class: 78 entries at one carrier need 78 codes, so seven bits are necessary whatever the reduction does. `namedResolution_eq_of_constant_on_classes` is the control as a theorem — a code that never separates two entries sharing a carrier recovers exactly nothing, which is why the register label recovers 0 of the 283 while the name recovers all of them. |
| `MeasureView.lean` | a measure word as a measurement | The relative reading of `hot` built on `Cumulative.lean`: `staticLayer` is the concept the lexicon carries today, `measureReading` is the measurement alone, and `measureLayer = cumulative staticLayer measureReading` is the reading the package ships. `measureLayer_refines_staticLayer` is the widening -- nothing the lexicon says is given up -- and `measureLayer_least` that it adds no resolution beyond what keeping both readings forces. `boundary_measureLayer_staticLayer` says the gain is exactly the pairs the concept conflates and the measurement splits, and `hot_tea_star_mem_boundary` with `hot_tea_magnitude = 363` and `hot_star_magnitude = 44000` that the gain is not empty. `measureReading_not_refines_staticLayer` is the rejected design failing: keeping only the measurement conflates two words the lexicon tells apart, which is the `violations = 66` the Python audit reports over its witness set (the shipped registers now measure every adjective, so the failure has to be exhibited rather than met by accident). `boundary_empty_of_unmeasured` is why the query must refuse where the registers hold no quantity, and `magnitude_strictMono` that a scale order is an order on magnitudes in every class at once. |
| `Comparative.lean` | *hotter than*, *as hot as* | The comparative is a relation between two **uses**, not between two words, which is why no reading of the concepts alone can answer one. `reading` drops the class and keeps the quantity and the magnitude; `Comparable` says two uses name magnitudes of the same quantity; `HotterThan` and `AsHotAs` are the comparative and the equative. `hotterThan_irrefl`, `HotterThan.asymm`, `HotterThan.trans` and `hotterThan_trichotomy` make it a strict order, trichotomous where it is defined, and `AsHotAs` an equivalence on measured uses. `hotterThan_iff_position_lt` is the exact agreement with the word order *within* one class; `coldStar_hotterThan_hotTea` and `comparative_not_determined_by_word_order` are the disagreement across classes -- *cold* for a star (8000 K) is hotter than *hot* for a cup of tea (363 K). `comparative_not_static` exhibits a pair the static reading conflates and the comparative separates, and `hotterThan_congr` that the comparative is a function of `measureLayer`, so the widening is exactly enough. `not_comparable_left_of_unmeasured` and `hotTea_not_comparable_fastWalking` are the two refusals, forced by the registers. |
| `Denotation.lean` | deciding what a name denotes | What a vocabulary decision can and cannot do to a repair. A `Vocabulary` is the arrangement in the abstract -- what the register dimensions on its own (`base`), what has been decided about the rest (`verdict`), what a quantity name's dimension is (`dimOf`), and which basis quantities carry one dimension to another (`factors`) -- and `reach` is the lookup with the decisions in hand. `reach_invents_nothing` says a decided name reaches a dimension only by naming an entry the register already holds, so a denotation can no more extend the register than an alias can. `secondPass_eq_firstPass_of_decided` says the second pass never *revises* the first, and `secondPass_eq_firstPass_of_no_quantity_verdict` that with no `quantity` verdict nothing new converts -- the content of the measured `converted = 0`. `undecided_is_decided` is the closure claim: once every endpoint carries a verdict, a triple left unclassified is one whose endpoint was decided not to be a quantity. `repaired_not_converted` says the one extra repair rule applies only where the dimensional rules declined, so the three outcomes partition the residue. The last section instantiates all of it on the three cases the measurement turns on: *gravity*, decided to be the register's `gravitational_field` and still declined against *mass* for want of a factor; *motion*, decided to be ambiguous and therefore left alone; and *move*, a process beside a velocity, which is repaired. |
| `Recipe.lean` | the recipe itself, as an object | A `Spec` is a **domain description** — the coordinates it holds, how each one is derived from an object, and which of them recover it — and everything the recipe used to produce by hand is a function of it. `readingOn` generates the reading of a selection as a `Layer`, so the machinery of `Layers.lean` and `Cumulative.lean` applies without knowing what the domain is about: `readingOn_mono` is that widening gives nothing up, `readingOn_append_least` that the widened reading is exactly the cumulative layer of its parts, and `boundary_readingOn_nonempty_iff` that what a widening *gains* is precisely a pair the narrower selection conflates and a new coordinate splits. `lossless_readingOn_iff`, `lossless_full_of_keys`, `encode_injective_of_keys` and `rebuild_encode` are the read-back: a description whose keys determine the object gives a lossless encoding with an exact inverse. `answer` with `answer_eq_some_iff` and `answer_eq_none_iff` is the query surface and its refusal boundary, read off the description and decidable — the `derive` query kind's refusal, proved. `encode_congr`, `indist_congr` and `answer_congr` are **regeneration**: two descriptions agreeing on the coordinates agree on the carriers, on the reading (hence on its classes, hence on any figure counted off them) and on every answer, which is the formal content of deleting a domain and rebuilding it from its description. `ratioSpec` instantiates all of it on the harmonic register's description: `ratioSpec_full_lossless`, `ratioSpec_product_not_lossless` (Tenney height alone conflates 3/2 with 6), the two boundaries, and `ratioSpec_refuses_cents`. |
| `Question.lean` | the question shape itself, as an object | A `Spec` is a **question description** — an opening `Phrasing`, named `Slot`s, and the words that separate them — and `matchPieces` is the one matcher over it, knowing nothing about coordinates, measure words or tasks. `matchPieces_rendered` is the **round trip**: a question written from a shape and a filling matches back to exactly that filling, under the conditions collected in `Clean`. `matchPieces_required_nonempty` is that the matcher never fills a required slot with nothing and carries on, so a refusal is informative. `matchPieces_lit_none` and `matchPieces_no_separator` are the two refusals as theorems — a question that does not open the shape, and a missing separator with something required after it — and `matchPieces_adjacent_holes` refuses a shape whose split would be a guess. `Phrasing.not_both_matchAt` and `matchPieces_not_both` are that **openings decide the shape**: with no opening a prefix of another's, no question enters two shapes, so the described shapes are a set rather than a priority list. `deriveShape` instantiates all of it on the shipped `derive` question — five openings, three separators, an optional domain tail — with `deriveShape_span_ratio_of_tea`, `deriveShape_what_derives_in_harmonics` and the two refusals decided by computation. |
| `QuestionNested.lean` | three more pieces of description language | The parts of a question that are not slots. A `ListCut` is a **described list** — the separator phrasings and the one admitted mark — and `cut_two` is its round trip: a list written as one item, a separator and another, where neither item holds a separator, cuts back to exactly those two, with `sepAt_shorter` the reason the cut terminates and `cut_ne_nil` / `cut_append` the two facts it rests on. A `ModifierFrame` is a **described modifier**, a word that directs the reading rather than naming an operand, and it is removed exactly twice: `strip_head` and `strip_frame` take it off the head and out of the trailing frame, and `strip_middle` leaves it where it stands inside an operand, because deleting it there would change the equation being audited. A `DegreeOperator` is an operator *formed* from a word rather than listed (`<word>er than`, `as <word> as`), and a `NestedSpec` is a **nested shape**: an operator between two operands that must each match a shape. `run_rendered` is the round trip for it, and `run_no_operator` and `run_side_refused` are its two refusals as theorems — the second is the boundary that keeps `sqrt(2) greater than 7/5` out of the comparative, because forming the operator is not enough when a side is not a measured use. `compareCut`, `tensorModifier` and `comparativeShape` instantiate all three on the shipped surfaces, decided by computation. |
| `Semantics/Meaning.lean` | the meaning space | `decode_coords` and `coords_injective`: the 24-coordinate meaning carrier round-trips and separates distinct well-formed meanings, so `semantics/meaning.py`'s encoding loses nothing. `encode_indep_of_notation`: an encoding that is a function of the meaning gives equal carriers to two notations of one subject — the property the spelling-hashed legacy carriers do not have. `formula_capacity_collision` and `capacity_forces_refusal`: past five formula slots two distinct meanings share a carrier, which is why the encoder refuses instead of truncating. |
| `Semantics/Grounding.lean` | meaning versus spelling, and the EXT10 → SI7 boundary | `semantic_iff_respects` and `spelling_not_semantic`: a map is a function of meaning exactly when it agrees on co-denoting notations, and a spelling-derived one is not, whatever it computes. `legacy_threshold_dichotomy`: no proximity radius on the legacy carriers recovers synonymy — every radius is either not semantic or trivially true of every pair. `si7_conflates_energy_torque` with `exists_visible_dim_not_si7`: the EXT10 → SI7 step is a boundary in the sense of `Layers.lean`, and energy and torque are the witness. |
| `DeltaSigma.lean` | the moving carrier | `dsAverage_error_le`: after `N` ticks the one-bit modulator's time average is within `1/N` of its target, and `dsAverage_tendsto` takes that to the limit — so a finite carrier that is allowed to move reaches *every* real, irrational included. This is the `1/N` law `reasoning/exact_real.py` reproduces at `N = 10, 100, 1000`. |
| `Irrational.lean` | the wall, and the tower that gets round it | `no_countable_layer_lossless`: a layer whose views form a countable set conflates two distinct reals, so no carrier holds an irrational — a cardinality argument, not an engineering limit. `towerView_injective`: what is lost at every single level is not lost by the tower as a whole. This is why a real is held as a process and its level-`n` stand-in `⌊x·2ⁿ⌋/2ⁿ` is only ever a stand-in. |
| `Reachable.lean` | what the 24-D carrier can hold | `avgVec_mem_hull`: every reading of the dynamic carrier is a convex combination of codewords, so the reachable set is inside the convex hull of the code. `not_tendsto_avg_of_separating`: a single linear functional separating a target from every codeword rules out convergence under *any* quantiser rule — the certificate the package computes for the ramp target. `avgVec_periodic` pins the set from the other side: a carrier cycling through `N` states reads back exactly the mean of its cycle. |
| `Computable.lean` | what is computable about an approximated value | `nonzero_iff_witness`: a real is nonzero **iff** a bound `|x| ≥ 2⁻ᵐ` exists, so that witness is precisely the information division needs; `inv_error_le` is the cost the implementation pays for it; `witness_depth_not_uniform` shows no fixed search depth works for every divisor, which is why `real_expr.divide` refuses by naming its depth. `eq_of_forall_abs_sub_le`: processes never separated are equal — but "never" quantifies over all precisions at once, which is why equality is refused and inequality is decided. |
| `Transcendental.lean` | the error budgets of `exp`, `log`, `sin`, `cos` and `x ^ y` | `exp_error_le`, `sin_error_le`, `cos_error_le` and `log_error_le` are the Lipschitz-style bounds `reasoning/transcendental.py` divides its precision among: the exponential's constant is `exp (max x a)`, the two trigonometric functions cost one extra bit, and the logarithm's constant is `1/c` for a lower bound `c` — which is why it needs a positivity witness. `pos_iff_witness` says such a witness is exactly what positivity is, the same shape as `nonzero_iff_witness` for division. `rpow_eq_exp_mul_log` is the power route, and `rpow_natCast_eq_pow` says the split between an integer power and a real one is a choice of algorithm, not of meaning. |
| `Tower.lean` | "does this continue?" | Yes: `dyadicLayer` is an explicit infinite tower that is cumulative, strictly gains expressive power at *every* step, has no lossless top, and still separates any two distinct carriers. Contrast `boundary_above_rational_empty`: whether a particular tower terminates is a property of the carrier space, not of the idea of layering. |
| `Golay/Code.lean` | the concrete extended binary Golay code | The code is built from the same parity block the package ships in `substrate/mog.py`: a word is a `Finset (Fin 24)` and `syn` is its 12-bit syndrome. `syn_symmDiff` makes the syndrome additive over symmetric difference, and `syn_eq_iff_isCodeword_symmDiff` says two words share a syndrome exactly when they differ by a codeword — the coset algebra everything else runs on. |
| `Golay/Sextet.lean` | the covering radius and the six-fold tie | Five exhaustive finite checks over all 4096 syndromes give `golay_min_weight` and hence `golay_min_distance_eight`; from them `unique_nearest_of_le_three` (reading is unambiguous up to weight 3), `covering_radius_eq_four`, and the shape of the ambiguity at weight 4: `ties_card_eq_six` — a deep hole has **exactly six** nearest codewords, no more and no fewer — with `sextet_partition` showing the six tie words partition the 24 coordinates into six tetrads, and `ties_pairwise_hdist_eight` that they are pairwise as far apart as the code allows. `coset_dichotomy` says every coset is either uniquely readable or a six-fold tie. |
| `Golay/Census.lean` | how often the tie happens, and where the average word sits | `coset_census` counts the 4,096 cosets by distance to the code — `1, 24, 276, 2024, 1771` — so `unique_vs_ambiguous` splits them into 2,325 uniquely readable and 1,771 six-fold ties, and `mean_coset_weight` computes the exact average distance `3433/1024`. `mean_coset_weight_gt_three` and `mean_coset_weight_lt_four` place it strictly between the packing radius 3 and the covering radius 4: the *average* word is already past the radius inside which the reading is unique, so ambiguity is the typical case for this code rather than a corner case. `cosetWt_eq_dist` identifies the coset weight with the distance to the nearest codeword. This is what `substrate/superposition.py`'s `coset_census_report` recomputes. |
| `Golay/Dynamics.lean` | the dynamical half of the criticality question | The census is about a distribution; this is about a process. One tick adds a parity-check column to the carrier's syndrome, so repeated one-bit perturbation is a random walk on the 4,096 cosets. `step_unif` and `stationary_unique`: the uniform law is stationary and is the *only* stationary law, so the long-run average distance to the code is the census figure `3433/1024` (`expect_unif_cosetWt`), with `3795/4096` of the mass at distance 3 or 4. But `par_col` and `iterate_dirac_ne_unif`: every column has odd parity, so the law alternates between the two parity classes and is **never** uniform — the chain has no limiting law; `prob_unif_subcritical_pos`: the stationary law keeps `301/4096` below the packing radius, so the weight does not lock on; and `perturb_correct_returns`: a corrected one-bit error returns the same codeword, so a corrected carrier stays *on* the code. The self-organised-criticality claim is therefore true only in its time-averaged form. The Cesàro statement the file leaves open is proved next door. |
| `Golay/Cesaro.lean` | the positive half of the same question | The perturb-only chain never converges, so the honest dynamical claim is about **time averages**, and this file proves it with an explicit rate: for every probability law `μ`, every syndrome `f` and every `N ≥ 1`, `cesaro_converges` gives `|cesaro μ N f − 1/4096| ≤ 24/N`, and `cesaro_tendsto` reads that back as an ordinary `Tendsto` for anyone who wants it in that form. The proof diagonalises the chain by the characters of `Syn = (ZMod 2)^12` — which take values `±1` and so live in `ℚ`, keeping the whole argument in exact rational arithmetic with no analysis. It turns on four facts about the eigenvalue `lam s`: `lam_zero` (`= 1`, which is why the limit is `1/4096` and not `0`), `lam_le` (`≤ 11/12` for `s ≠ 0`, quantitative irreducibility, because the columns span every syndrome), `abs_lam_le_one` (attained at `−1` on the all-ones syndrome — the periodicity itself), and `abs_geom_sum_le` (the partial sums are bounded by `24`, which is the `24` in the headline bound). The orthogonality relation `sum_chi` is proved here rather than imported. |
| `Superposition.lean` | holding all six readings at once | `bundleF2_eq_one`: bundling the six tie words by XOR gives the all-ones vector *whatever the tie is*, so the F2 bundle of a superposition carries no information about it — `bundleF2_constant`. Over the rationals the same bundle is faithful: `bundleQ_eq` computes the mean coordinatewise as `(1 + 4·vᵢ)/6`, `bundleQ_recover` reads the tie back out of it and `bundleQ_injective` says distinct superpositions have distinct bundles. This is the exact sense in which the ambiguity survives in a rational carrier and dies in a binary one. |
| `Wobble.lean` | ambiguity as a moving carrier | `sextet_cycle_avgVec`: a carrier that cycles through the six tied readings is read back exactly as their rational bundle, `sextet_cycle_determines` that this reading still determines the tie, and `sextet_cycle_tendsto` takes it to the limit. Wobble between the candidates is therefore a lossless way to hold them, not noise. |
| `HullExpansion.lean` | when a wider alphabet is genuinely needed | `cycle_avgVec_eq` reads a cycle back as its mean; `concTarget_not_mem_hull_scaled` exhibits a linear certificate separating a target from the hull of the available states, so `concTarget_unreachable_scaled` rules it out under any schedule — and `concTarget_reached_by_leech` hits it exactly in 16 ticks once two more states are admitted. `alphabet_expansion_strictly_helps` is the two together: the gain is in the alphabet, not in the schedule. |
| `VOA.lean` | the state–field map `Y(u, z)` on the 2A algebra, and where the finite layer stops | The Griess product of `Sakuma.lean` is one mode of a vertex operator algebra's state–field map, `Y(u, z) = ∑ₙ uₙ z^{-n-1}`, and this file builds that map at the layer the finite algebra carries: `mode u 1 v = u ⋆ v` and nothing else, so `mode_truncated` makes the field a genuine formal Laurent series. What the layer *does* carry is real vertex-algebra structure: `mode_skew` is the skew-symmetry axiom at this weight, and the invariant form is not chosen but forced — `form_forced_off_diagonal` derives `⟨eᵢ, eⱼ⟩ = (1/8)⟨eᵢ, eᵢ⟩` from invariance alone — giving `form_invariant`, `mode_self_adjoint` and `form_nondegenerate`, so the layer is a Frobenius algebra with vacuum `vac = (4/5)(e₀+e₁+e₂)` (`vac_mul`, `form_vac = 12/5`). What it does not carry is stated just as exactly: `borcherds_commutator_fails` shows the commutator formula at `m = n = 1` would demand `u ⋆ (v ⋆ w) − v ⋆ (u ⋆ w) = (u ⋆ v) ⋆ w`, and on the axis triple the two sides are `(−3/32) e₀ + (3/32) e₁` and `(−3/32) e₂`. The discarded modes are load-bearing; the infinite-dimensional half of the Moonshine bridge is not built here, and nothing claims it is. |
| `Heisenberg.lean` | the infinite-dimensional half of the same bridge | The Fock space of one free boson over the exact rationals: `V = MvPolynomial ℕ ℚ`, `create i` multiplication by `xᵢ`, `annihilate i` the scaled derivative, and `a k` the `k`-th mode for every integer `k`. `mode_commutator` is the Heisenberg relation `⁅aₘ, aₙ⁆ = m δ_{m+n,0} · id` for all integers at once — the commutator formula the Griess layer of `VOA.lean` could not satisfy. `mode_truncated` says every state is annihilated by all sufficiently high modes, so the field really is a formal Laurent series, and `borcherds_commutator` writes the bracket in Borcherds' own form with the generalised binomial coefficient, the tail vanishing by `alpha_mode_eq_zero_of_two_le`. `no_finite_dimensional_model` is the obstruction, proved in general: in characteristic zero no pair of endomorphisms of a nonzero finite-dimensional space satisfies `⁅A, B⁆ = c · id` with `c ≠ 0`, because the trace of a commutator is `0`. Hence `fock_infinite_dimensional`, and `griess_layer_discards_nonzero_modes` names the discarded modes that do the damage. The infinite-dimensional half is therefore necessary rather than traditional. |
| `Mantissa.lean` | where floating-point drift comes from | The instrument is the doubling map `x ↦ 2x mod 1`, every step of which is exact in binary floating point. `dyadicOrbit_eq`, `dyadicOrbit_collapses`, `dyadicOrbit_zero_stays`: a dyadic rational with `k` bits after the point reaches `0` in `k` steps and stays — and a float *is* a dyadic rational, so a float's orbit always dies. `oddOrbit_ne_zero`, `oddOrbit_periodic`, `exists_period`: the exact orbit of `1/p` for odd `p > 1` never reaches `0` and is periodic, with period any `d` for which `2^d ≡ 1 mod p`. `dyadic_ne_odd_orbit` separates the two behaviours. All of the loss is spent in the single act of rounding, which is what `reasoning/mantissa.py` measures. |
| `Reversible.lean` | the read channel, the gates and the kinks | `gray_step` and `gray_single_bit`: consecutive Gray codes differ by exactly one bit. `sum_flipCount`, `binaryCycleFlips_eq`, `grayCycleFlips_eq`: over a full `w`-bit cycle binary counting flips `2^(w+1) − 2` bits and Gray counting `2^w`, so `gray_not_exactly_half` refutes the blueprint's "exactly half" at every finite width and `gray_two_mul_eq` gives the sharp statement `2·grayCycleFlips w = binaryCycleFlips w + 2`. `toffoli_involutive`, `fredkin_involutive` and their bijectivity make the gates erase nothing; `round_cubed` and `round_not_involutive` describe the composite round. The kink count carries information as defects in a circular string rather than as coordinate values, which is what `reasoning/reversible.py` measures at width 24. |
| `Cascade.lean` | noise as the computation: signal-driven and cascaded delta-sigma | `DeltaSigma.lean` chases a *constant* target; here the input is a signal. `mState_mem_Ico` and `mAverage_error_le`: the bits track a time-varying input's running mean to `1/N`, and `mState_const`/`mBit_const` recover the constant case. `mState_period_eq_zero`, `mState_periodic`, `mBit_periodic`: a `P`-periodic input whose period sums to an integer gives an exactly closed orbit — the wobble is a cycle, not a drift. The MASH 1-1 cascade is then built and measured: `casOut_mem` (the cascaded output takes four values, `−1 … 2`), `casOut_error` (its error is a *second* difference), and `casTriangular_error_lt` — under a triangular window the cascade's error is below `2/(M(M−1))`, against `firstOrder_triangular_error_ge`'s `1/(2M)` for a single loop on the same target. That `O(1/M²)` against `O(1/M)` is the law `reasoning/noise_lab.py` reproduces exactly. |
| `Sturmian.lean` | the modulator's stream as a mechanical word | The bridge is `dsState_eq_fract`: the accumulator after `n` ticks is exactly `Int.fract (n·t)`, so the loop is an irrational rotation and `dsBit_eq_floor_diff` makes the emitted stream the Sturmian word of slope `t`. Everything the external studies *measure* about that stream is then a closed form of the target: `dsOnes_eq_floor` (the ones in `N` ticks are exactly `⌊N t⌋`), `ds_zero_run_length_lt` / `ds_one_run_length_lt` (runs are shorter than `1/t` and `1/(1−t)`), `dsTransitions_eq` and `dsTransitions_rate_tendsto`, `dsMeanRunLength_tendsto`, and `ds_wobbleEntropy_tendsto` with `ds_wobbleEntropy_zero_iff_silent`; `ds_resonance_lock` and `ds_resonance_entropy` pin the locked loop at entropy exactly zero. This is why `reasoning/wobble.py` reports the law beside every measured column. |
| `Feedback.lean` | error feedback through a rational matrix, and the symmetry it must respect | A modulator on `n` coordinates whose past quantisation error returns through a matrix `A`. `efErr_abs_le_half`: the instantaneous error never leaves `[−1/2, 1/2]`, whatever `A` is. `efSum_eq` is the exact accounting identity, and at `A = 1` it collapses to `efAverage_error_le_identity` — `|average of the bits − running mean of the input| ≤ 1/(2N)` in every coordinate, the vector form of the `1/N` law and sharper than the scalar accumulator's. `halfFeedback_dead_zone` shows a contracted feedback is not merely slower: with `A = 1/2` on the constant `1/4` the quantiser never fires at all. `efOut_equivariant`: a permutation leaving `A` invariant permutes the whole trajectory tick for tick, so noise shaping through a symmetry-commuting matrix commutes with that symmetry exactly. |
| `HigherLattices.lean` | past 24: the 32- and 48-dimensional rungs, stated for any code with the right weights | Construction D in 32 dimensions is `mk a b c = 4a + 2b + c`: `norm_ge_of_ne_zero` proves every nonzero vector has `∑xᵢ² ≥ 16` by a three-way split on the resolution a vector is first visible at, and `norm_dvd_eight` proves `8 ∣ ∑xᵢ²` — the step where the duality `C₂ ⊆ C₁^⊥` does the work, through the even overlap `meetWt`. `mk_injective`, `mk_emod_two` and `mk_emod_four` are the addressing lemmas: a point determines its three levels, and the two coarse ones are read off by reducing mod 2 and mod 4. In 48 dimensions `even_norm_ge_eighteen` shows the even sublattice of the ternary Construction A reaches `∑xᵢ² ≥ 18`, that is the extremal minimum 6, with `norm_ge_card_support` and `norm_dvd_nine` as the two halves of the case split. Nothing mentions a particular code, so `substrate/lattice32.py` and `lattice48.py` supply Reed–Muller and Pless codes and inherit the theorems. |
| `ShellSigma.lean` | the delta–sigma loop run against the Leech shell rather than a scalar | The support function of a finite set of directions is a maximum, and the loop that chases it is the one `reasoning/shell_sigma.py` measures: the state stays bounded, the average error falls as `1/N`, and the reachable set is the hull of the shell — which is why a target outside that hull cannot be tracked at any schedule length. |
| `Harmony.lean` | why no tuning ever closes | The register of 28 intervals in `data_objects/harmonics.py` is measured against equal temperament interval by interval, and every error comes out non-zero; this file says why none ever could. `three_pow_ne_two_pow` is the kernel — a power of three is odd. `fifth_never_closes` makes the circle of fifths not a circle: `(3/2)^n ≠ 2^m` for every `n > 0` and every integer `m`, where `reasoning/harmony.py` only counts to `n = 200`. `odd_prime_ratio_ne_two_zpow` is the general obstruction and the one the register needs: a ratio in lowest terms carrying any odd prime is not a step of *any* equal division of the octave, for every number of divisions at once, so `tet_error` can read `1` only at the unison and the octave — `fifth_not_tempered`, `major_third_not_tempered` and `harmonic_seventh_not_tempered` are the three named corollaries, and `pythagorean_comma_eq`, `syntonic_comma_eq` and `fifth_tet_error` pin the exact residues the report quotes. |
| `LogBucket.lean` | a magnitude without a logarithm | The specification of the exact magnitude bucket the economic register reads its prices through, and of the control the economic study rests on. `bucket_spec` and `bucket_unique` say that `b ^ k ≤ x < b ^ (k + 1)` holds for the computed bucket and for no other integer — `exists_unique_bucket` is the pair as one `∃!` — so the descriptor is well defined without ever evaluating a logarithm. `le_iff_num_le` restates the bracket as the integer multiplications the code performs on numerator and denominator, `bucket_mono` makes ordering by bucket an ordering by magnitude, `mantissa_mem_Ico` puts the mantissa kept beside it in `[1, b)`, and `bucket_zpow` with `mantissa_zpow_eq_one` gives the scale-invariance the descriptor claims. `distSq_smul` with `order_preserved_by_scaling` is the control: scaling every coordinate by one positive factor cannot reorder distances, so the undecoded comparison is a single set of numbers for the whole sweep rather than one per scale. |
| `Address.lean` | what a lattice address can and cannot mean | The specification of `reasoning/lean_address.py`, which gives every declaration of this development a deterministic Leech address. A `Quantiser L ρ` is a nearest-point map with a covering bound; `Quantiser.dist_le` makes addressing 1-Lipschitz up to `2ρ`, `Quantiser.ne_of_far` says points more than `2ρ` apart cannot share an address, and `Quantiser.fixed` says a point already in `L` is its own address. `address_congr` is the honest limit on meaning: equal features force equal addresses, so no distinction can live in the address book that the feature map has already discarded — `Conflates` is the resulting equivalence and `injective_features_of_injective_address` runs the implication the only way it goes. `readback_unique` is the encoding's fidelity: with scale above `2ρ`, at most one feature vector is compatible with an address, so the address *is* the feature vector. `eightZ_mem_leech` proves `8ℤ²⁴ ⊆ Λ`, which is why scale 8 would make the decoder an identity map, and `nineZ_not_mem_leech` shows scale 9 does not. |
| `LLVQTable.lean` | the class minimum of the quantiser's lookup table, and why bounding the search is exact | The specification of `reasoning/llvq_table.py`, which replaces the Leech quantiser's scan over 4,096 Golay codewords by the MOG's own class structure. A class is a choice of one of two patterns per column, and `cost_eq` is the identity the whole table rests on: any choice costs the greedy choice plus the gaps of the columns where it differs. `isLeast_cost_of_parity_eq` is then the class minimum when the greedy choice already has the parity the class requires, and `isLeast_cost_of_parity_ne` is the one-column repair when it does not — flip the column of least gap, and `exists_least_gap` says there is always one to flip. `parZ_update` is the fact that makes the repair a repair: flipping one top bit flips the parity. `card_parity_class` counts a class, `2 ^ (n - 1)` choices of each parity, which is the 32 codewords per class the enumeration finds. `isLeast_of_bounded_search` is branch and bound stated once and for all: if `f` bounds `g` below and some element of the searched part already attains `g` no worse than `f` anywhere outside it, the search may stop — which is why the decoder may stop after a handful of the 256 (congruence class, class) pairs and still be exact. |

## The retrieved files

Twenty-five of the files above came back from the supplied archive in the
retrieval round, which is written up in
[`studies/RETRIEVED_LEAN_STUDY.md`](../../../../studies/RETRIEVED_LEAN_STUDY.md)
— 7,170 lines and 848 declarations, a third of the development. The study's
table says what each one claimed and what is now proved; the summary is:

| file | what it settles |
|---|---|
| `Calibration.lean` | the EM calibration chain returns the `c` it was given (`substrate_c_is_circular`); an action and an energy never generate a velocity; what survives is the dimensionless propagation law and the octad minimum of the tax |
| `AlignmentPoints.lean` | the two alignment points that are not fits, audited — including a corrected error bar on the electron-mass point |
| `FitCapacity.lean` | how much a numerical agreement can prove: `fit_capacity`, and the bit ledger that scores the archive's three headline fits at under one bit, three to four, and two to three |
| `Packing.lean` | what a binary substrate forces is **23**; 24 is a parity extension, and the extra coordinate buys detection, never correction |
| `Triad.lean` | `3, 6, 9` is one number and any three-element set produces it; 24 is decomposition-rich, so matching a decomposition of it is not evidence |
| `SeedLayers.lean` | where each seed may enter: no finite linear symmetry has a transcendental character value, `π` is excluded from a lattice by integrality, and `φ` enters as an infinite-order stretch |
| `StepCost.lean` | the coherence ladder is gauge-independent, a constant rate is not a clock, and the 13-step shortcut is a strict distortion of the word metric |
| `SpatialArithmetic.lean` | the polygon codec is lossless with the sign in the parity of the vertex count, operator clearance is forced by the triangle inequality, and the totient sub-cycle law `⌊N/2⌋ − φ(N)/2` is proved |
| `ReasoningLoop.lean` | the ARC-era cognitive cycle: the verification gate holds, refusal is informative, and passing the gate still does not determine the answer |
| `Foundations.lean`, `Gen2.lean`, `Gen3.lean` | the three generations of the paper's formal companion: the mod-2 ceiling and its rational strengthening, the derived carrier, the plus-type count, the extraspecial group built from its cocycle, and the ledgers |
| `Cube/Surface.lean` | the cube's 24 surface cells *are* the MOG grid: 4096 codewords, enumerator `1, 759, 2576, 759, 1`, minimum weight 8, and the one-face/two-face erasure boundary |
| `Cube/HexTiles.lean` | the hexacode layer as a tiling: three incoming faces determine a tile, the update matrix has order three, and an assembly is fixed by its boundary |
| `Cube/Stabiliser.lean` | only 12 of the cube's 48 symmetries preserve the code under the MOG identification — but a different placement is preserved by all 24 rotations and no improper symmetry |
| `Golay/CubeMirror.lean` | and 24 is the ceiling: a diagonal mirror fixes 4 cells and transposes 10 pairs, leaving 220 invariant five-sets while every invariant octad holds 0, 6 or 12 of them — six does not divide 220, so *no* Golay code on the surface survives a mirror |
| `Cube/Tax.lean` | the price list: covering radius exactly 4, so the worst repair costs `4Q`; unique below 3, ambiguous at 4; AND is priced |
| `Cube/Three.lean` | the three-cube rule proposal gives a `[24,12,4]` code and no relabelling repairs it — and Turyn's glue on the same three cubes does give the Golay code |
| `Shortcut/*.lean` | the lattice-shortcut method, corrected: the complete Golay decoder the substrate's `snap_to_codeword` is not, the Gray layer's `O(1)` jump formula, the Leech octad step, and an audit of the published directory whose "even quantisation" turns out to be true by construction |

## Relationship to the Python package

Nothing here imports or executes Python, and nothing in the package imports
Lean. The correspondence is by statement:

* `Constants.lean` states the constants table of the top-level README;
* `GolayBoundary.lean` states what `substrate/golay_decode.py` reports as
  `corrected` / `ambiguous`;
* `Permutation.lean` and `Endianness.lean` state the frame facts that
  `glm_universal/migration/frames.py` audits numerically;
* `Facets.lean` states the decomposition `reasoning/facets.py` computes;
* `Layers.lean`, `Stack.lean` and `Tower.lean` state the theory that
  `reasoning/information_loss.py` and `reasoning/dimension_layers.py`
  instantiate;
* `Semantics/Meaning.lean` and `Semantics/Grounding.lean` state what
  `glm_universal/semantics/` implements — the round trip and injectivity of
  `meaning.encode`, the capacity refusal, the sense in which the inherited
  `sha256`-of-a-spelling carriers cannot be a measurement of meaning (which
  `semantics/audit.py` measures numerically), and the `si7_conflates` relation
  the grounded graph records;
* `DeltaSigma.lean`, `Irrational.lean`, `Reachable.lean` and `Computable.lean`
  state what `reasoning/exact_real.py` and `reasoning/real_expr.py` implement —
  the `1/N` law of the modulator, the cardinality wall and the faithful tower
  of stand-ins, the convex hull that bounds the 24-D carrier together with the
  separating certificate, and the exact sense in which comparison, division and
  equality are and are not computable;
* `Transcendental.lean` states the error budgets `reasoning/transcendental.py`
  pays — the argument precision each of `exp`, `log`, `sin`, `cos` and a real
  power demands, and the positivity witness `log` refuses without;
* `Cumulative.lean` states the property `dimension_layers.LAYERS` was made to
  have — `LAYER_INTEGER` keeps `substrate_bits`, the Griess and universal
  measures keep the carrier term — and the failure of the reading kept beside
  it as `LAYER_INTEGER_RAW`, which
  `information_loss_report()["non_cumulative"]` measures;
* `LayerChain.lean` states the same property of the layers themselves, over the
  24-coordinate carriers `information_loss.py` runs on, so that
  `refinement_chain_intact` has a theorem behind it and not only a measurement;
* `Golay/Code.lean`, `Golay/Sextet.lean`, `Golay/Census.lean`,
  `Golay/Dynamics.lean`, `Golay/Cesaro.lean`, `Superposition.lean`,
  `Wobble.lean`
  and `HullExpansion.lean` state what `substrate/superposition.py` implements —
  the six-fold tie at a deep hole and its sextet partition, the XOR bundle that
  collapses to the all-ones vector against the rational bundle that does not,
  contextual collapse back to a single reading, the wobble cycle whose time
  average is that rational bundle, the coset census and the exact mean coset
  weight `3433/1024` that `coset_census_report` recomputes, the perturbation
  chain that `coset_chain_report` pushes forward exactly, and the separating
  certificate behind the alphabet-expansion report;
* `Sturmian.lean` states the laws `reasoning/wobble.py` prints beside its
  measured columns, and through it the `report signature` subject and the
  spectral-signature section of `GLM_STUDY_CATALOG_AUDIT.md`: the entropy, the
  run-length bounds, the transition rate and the mean run length are
  consequences of the target rather than findings about the stream;
* `Harmony.lean` states why the harmonic register's tempering errors are
  what `reasoning/harmony.py` measures: the Python side counts the fifth's
  failure to close to `n = 200` and reports one exact error per interval,
  and the Lean side closes both for every `n` and every equal division of
  the octave at once — which is what `report harmony` cites in its second
  and third columns;
* `Cascade.lean` and `Feedback.lean` state the two directions the modulator is
  pushed in beyond a single scalar loop, and `reasoning/noise_lab.py` measures
  both: cascading, and the vector loop whose error returns through a rational
  matrix — `quantise`, `identity_matrix`, `feedback_tracking`,
  `equivariance_check` and `dead_zone` are named after the definitions and
  theorems here;
* `Recipe.lean` states the path `glm_universal/recipe/build.py` walks for every
  domain description: the Python side measures that three registers come back
  identical from their descriptions, and the Lean side says why any two
  descriptions that agree on the coordinates must — `encode_congr`,
  `indist_congr` and `answer_congr` — together with the widening, the
  read-back and the refusal boundary the `derive` query kind reports.

* `Question.lean` states the matcher `glm_universal/language/build.py`
  walks for every question description: the Python side measures that the
  described shapes and the hand-written parser make the same query out of the
  same question over a generated corpus, and the Lean side says what is not a
  measurement — writing and reading are inverse, a required slot is never
  silently empty, and disjoint openings make the order the shapes are tried
  in irrelevant.

* `QuestionNested.lean` states the three pieces of description language that
  are not slots — a list, a modifier and a nested side — that
  `glm_universal/language/build.py`, `infix.py` and `nested.py` read: the
  Python side measures the agreement with the branches those descriptions
  replaced, and the Lean side says what is not a measurement — cutting a list
  inverts writing one, a modifier comes out at the head and in the trailing
  frame and nowhere else, and a nested shape refuses a side that is not a use
  of the shape it nests.

Where the two disagree, the Lean file is the specification and the Python is
the implementation under test.
